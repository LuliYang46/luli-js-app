# Finding the Fermat Point

A dependency-free interactive SVG lesson demonstrating the 60° rotation construction for the Fermat point of a triangle.

Open `index.html` directly in a modern browser. No build step or web server is required.

Run the geometry checks with:

```sh
node --test geometry.test.js
```
